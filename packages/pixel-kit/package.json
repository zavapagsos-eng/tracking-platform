{
  "name": "@tracking/pixel-kit",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "description": "Shared, sandbox-safe logic for Shopify Web Pixel extensions (Store A and Store B): first-party identity cookies, attribution capture, and fail-open Gateway delivery. Pure functions only — no store-specific event mapping here (see each extension's own eventMapper.ts).",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@shopify/web-pixels-extension": "2.18.0",
    "@tracking/schema": "0.1.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.3"
  }
}
